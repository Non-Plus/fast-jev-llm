# Third-party notices

fast-jev-llm is MIT-licensed first-party software.

The published npm package (`fast-jev-llm`) ships a compiled CLI bundle
with **no production npm dependencies**. Runtime is Node.js 22+, which
is not redistributed in the tarball.

Development tools (TypeScript, Vitest, esbuild, tsx, pnpm) are
dev-only and are not included in the published package.

Inspiration (not bundled source):
[fast-jev-compaction](https://github.com/tamaratran/fast-jev-compaction)
(MIT). This repository does not copy that project’s source.

This file is not legal advice and does not claim license
compatibility beyond the packages actually shipped.
